import NextTopLoader from "nextjs-toploader"

export default function () {

    return (
        <NextTopLoader
            color="var(--main-color)"
            showSpinner={false}
        />
    )
}